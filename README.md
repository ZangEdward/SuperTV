# SuperTV 原生安卓重构项目 (LunaTV & Selene 增强版)

本项目致力于将基于 Expo/React Native 的 `supertv` 项目重构为**现代 Android 原生应用 (Kotlin + Jetpack Compose + Material3)**。

## 目录结构
```text
app/src/main/
├── java/com/supertv/app/
│   ├── ui/                 # UI 层 (Compose)
│   │   ├── components/     # 共享 UI 组件 (已实现 Paging 3, UserMenu, AI对话框)
│   │   ├── transform/      # 首页分区布局 (带 Lucide 分类导航)
│   │   ├── search/         # 搜索界面 (双模式：聚合 & 网盘)
│   │   ├── detail/         # 详情界面
│   │   ├── player/         # 播放器界面 (手势控制与选集弹窗)
│   │   └── slideshow/      # 缓存管理界面 (原生任务列表)
│   ├── data/               # 数据层 (Retrofit, Store, ApiNodeService)
│   ├── services/           # 原生业务逻辑服务 (Search/M3U/AdFilter/Dlna/AI/CrashHandler)
│   └── model/              # 数据模型定义 (集成 LunaTV 增强模型)
├── res/                    # 资源文件 (基于 Lucide 风格重制的图标系统)
└── assets/                 # 动态注入配置 (api_nodes.json)
```

## 最新更新与修复
- [x] **搜索并发优化**: 重构了 `SearchEngine`，采用 Kotlin 协程 `async` 并发请求所有视频源，极大地提升了搜索速度，解决了获取信息慢的问题。
- [x] **网盘搜索修复**: 更新了 `NetDiskResponse` 模型以对齐 LunaTV 接口，现在“盘搜”功能可以正常解析并展示搜索结果。
- [x] **乱码彻底修复**: 在 `RetrofitClient` 中引入了强制 UTF-8 解码的拦截器，解决了服务器节点名称在 Android 原生端显示乱码的问题。
- [x] **导航与详情页修复**: 
    - 实现了点击影视海报进入详情页的完整导航逻辑。
    - 详情页播放源采用**竖向滑动排布**，优化了在大屏幕和列表下的选择体验。
- [x] **全局 Header 深度重构**: 
    - 移除了顶部的 APP 名称，改为居中式的**现代化搜索框**。
    - 左侧集成**用户头像/菜单**，点击可进入功能丰富的用户中心。
    - 右侧集成**下载管理按钮**，快速查看离线任务。
    - 新增**明暗主题切换**功能，支持深色与浅色模式的一键无缝转换。
- [x] **全场景转场动画**: 修复了页面切换动画缺失的问题。现在所有页面跳转（包括底部标签切换、详情页进入、搜索打开等）均支持平滑的**左右滑动效果**（首页切换除外，保持稳定感）。
- [x] **直播页修正**: 将原本显示错误的直播页重构为**电视直播 (TV Live)**，整合了央视与主流卫视频道列表。
- [x] **用户菜单功能完备**: 仿照 Selene 完善了用户中心，支持**观看历史**、**我的收藏**、**节点测速切换**、**AI 智能推荐**以及**上映日历**等核心功能。
- [x] **高性能缓存 (秒开)**: 引入了基于 `SharedPreferences` 的数据持久化层，首页及分类频道实现**秒级启动加载**，并在后台静默刷新数据。
- [x] **登录逻辑收紧**：移除了本地演示模式（跳过按钮），强制用户登录以访问受限资源。
- [x] **用户信息显示**：修复了点击头像后用户菜单中用户名显示不正确的问题，现在能正确显示当前登录用户。
- [x] **搜索稳定性提升**：
    - 修复了无网络环境下点击搜索按钮导致的闪退（Crash）。
    - 修复了搜索结果无法正常显示的问题，提升了搜索的响应率。
- [x] **API 链路优化**：解决了底部标签数据加载 404 的问题，统合了 API 路由规则。
- [x] **UI 极致精简**：移除了首页悬浮的信封按钮，界面更加清爽。
- [x] **导航逻辑修复**：全局适配物理返回键/手势返回，确保所有界面按返回键均能正确回退至上一级页面。
- [x] **服务器测速还原**：同步 `supertvold` 的测速逻辑，使用客户端原生 `HEAD`探测，解决了节点提示“不可达”的问题，提供实时的延迟反馈。
- [x] **核心搜索逻辑对齐**: 
    - 引入了 `SearchUtils`，完美复刻 `supertvold` 的去空格、去符号及结果合并逻辑。
    - 实现了“精准匹配 -> 去尾搜索”的两阶段激进检索策略，显著提升了资源召回率。
- [x] **元数据优先聚合**: 
    - 首页及分类页优先加载豆瓣/Bangumi 的精美元数据（海报、评分）。
    - 自动触发后台异步匹配，将元数据项目与自有 API 播放源关联，解决了纯元数据源无法播放的问题。
- [x] **全局主题与 UI 优化**:
    - **真全局深色模式**: 重构了 `MainActivity` 布局，所有顶栏、底栏及容器均完美适配明暗切换，解决了搜索栏背景色不跟随的问题。
    - **底栏无缝衔接**: 修复了底部导航栏容器在部分机型上出现的漏角/边缘偏移问题，实现了全屏铺满效果。
    - **自适应栅格布局**: 分类页网格列数现在根据屏幕方向（横屏/竖屏）自动调整（3列或5列），提供更佳的视觉体验。
- [x] **数据统计中心**: 新增“数据统计”功能，可实时查看收藏数、历史记录数、搜索次数及缓存集数。
- [x] **盘搜功能大升级**: 
    - 仿照 `supertvold` 实现了网盘资源分类，支持夸克、百度、阿里、磁力等多种资源的一键切换。
    - 新增“复制链接”与“直接打开”功能，完美对接移动端浏览器与下载器。
- [x] **移动端视觉统一**: 
    - 重构了移动端搜索卡片，将原本拥挤的 3 列布局优化为 2 列大图模式。
    - 统一了海报角标样式（评分、来源、集数），视觉观感对齐 Selene 风格。
- [x] **全网激进检索 (详情页增强)**: 
    - 解决了豆瓣/Bangumi 等纯数据源无法播放的问题。进入此类详情页时，App 会自动以后台方式触发全网同名资源搜索。
    - 支持全网源自动合并与测速，智能提示“全网检索中...”，并自动加载第一个有效播放源。
- [x] **智能测速选集**: 详情页新增播放源实时测速功能，直接在源列表显示延迟（ms），并根据快慢进行颜色分级，确保极速观影。
- [x] **缓存持久化优化**: 将离线下载目录从系统临时目录迁移至应用专用外部存储空间，解决了应用关闭或系统清理时缓存丢失的问题。
- [x] **全方位稳定性保障**:
    - **导航容错**: 优化了 Navigation Action 的分发逻辑，彻底修复在首页分类频道点击剧集卡片导致的闪退问题。
    - **乱码深度兼容**: `ApiNodeService` 新增启发式编码检测，支持 UTF-8 与 GBK (ANSI) 自动切换。
    - **开发者知识库 (上下文记忆)**: 记录了核心逻辑架构及避坑指南。
- [x] **UI 布局深度优化**:
    - **彻底解决双顶栏**: 移除 Fragment 内部渲染的冗余 Header，统一由 `MainActivity` 控制全局固定顶栏。
    - **首页沉浸式修复**: 修正了首页分类内容被顶栏遮挡或出现重复偏移的问题。
    - **分类页视觉升级**: 除了“热门”外，电影、剧集、动漫、综艺及短剧频道均改为 **3 列网格 (Grid)** 竖向展示，浏览效率大幅提升。
    - **动画物理感增强**: 修复并增强了底部导航栏与首页分类标签的左右滑动动画，方向完全匹配标签顺序。
- [x] **播放源逻辑补全**: 进一步对齐 `supertvold`，确保豆瓣/Bangumi 等元数据源在进入详情页时能正确通过全网搜索补全播放源。

## 开发者知识库 (上下文记忆)

为了方便后续维护和上下文清除后的快速恢复，以下是本项目核心逻辑的“阅读经验”：

### 1. 架构关联
*   **后端对齐**: 本项目逻辑深度参考了 `.gradle/SuperTV_old-master`。`SearchViewModel` 对应原项目的 `stores/netdiskStore` 和 `stores/searchStore`。
*   **数据流**: 搜索采用 **Paging 3** 分页。网盘搜索结果是 `Map<String, List<NetDiskItem>>`，需要按 Key 分类展示。
*   **双模式详情**: 豆瓣/Bangumi 源仅提供元数据。进入详情页时，必须调用 `SearchViewModel.loadAllSources()` 触发全网激进检索，以补全播放链接。

### 2. 核心避坑指南
*   **编码问题**: `api_nodes.json` 经常在 Windows 下被保存为 GBK。`ApiNodeService` 必须包含 `\ufffd` 启发式检测或多重编码尝试读取。
*   **图片防盗链**: 豆瓣图片必须带 `Referer: https://movie.douban.com/` 和特定 UA，由 `ImageUrlHelper` 统一处理。
*   **导航陷阱**: 本项目多个标签页共享 `TransformFragment`。在 XML 中定义的 Action（如 `action_nav_tv_to_detail`）必须与当前所在的 `id` 匹配，否则会抛出 `IllegalArgumentException`。在 `onItemClick` 中应动态判断 `findNavController().currentDestination?.id`。
*   **沉浸式适配**: `GlobalHeader` 在搜索和详情页应隐藏。底部 `ComposeBottomNavBar` 需要手动处理 `windowInsetsPadding(WindowInsets.navigationBars)` 且背景 `Surface` 需额外高度以防系统小白条处漏色。

### 3. 动画逻辑
*   **方向感交互**: 页面切换动画（`slide_in/out`）并非固定。在 `MainActivity` 和 `TransformFragment` 中，通过对比 `targetIndex` 与 `currentIndex` 来决定是从右往左还是从左往右滑。

### 4. 数据源与播放源逻辑 (2025-05-22 更新)
*   **数据源分类**: 豆瓣、Bangumi 仅作为**元数据源**（提供海报、评分、简介），视频 CMS 站点作为**播放源**。
*   **详情页补全策略**: 进入详情页时，若 `source` 为 `douban` 或 `bangumi`，客户端改用专用接口（`/api/douban/details` 或 `api/proxy/bangumi`）获取元数据，并同步触发全网搜索。
*   **自动合并**: 全网搜索结果会自动与当前元数据详情合并。若当前详情无剧集（数据源），将自动取第一个搜索结果的剧集列表进行填充，实现了“点开豆瓣海报即刻搜源播放”的无缝体验。
*   **激进搜索算法增强**: 改进了 `SearchUtils.generateSearchVariants`。支持“空格消除”、“中文数字归一化（如：第四季 -> 第4季）”、“渐进式词组截断”等多种变体并行搜索，大幅提升了资源命中率，解决了“搜索搜不到”的问题。
*   **网盘搜索修复**: 完全兼容 `LunaTV-Enhanced` 后台的 `merged_by_type` 数据结构。修复了 `NetDiskResponse` 解析失败导致的结果为空问题，现在支持按云盘类型（阿里、夸克、百度等）分类展示结果。
*   **TV 端搜索对齐**: 解决了 TV 端点击海报无法进入详情或进入后无剧集的问题，逻辑现在与移动端保持一致。
*   **详情页播放源逻辑优化**: 彻底分离了元数据源（豆瓣/Bangumi）与播放源（后台 CMS 采集站）。进入详情页后，系统会自动过滤掉重复的元数据结果，仅展示真实的视频播放源，并自动合并最佳（集数最多）的播放源到当前页面。
*   **顶部导航栏 UI 进化**: 
    - 降低了 `GlobalHeader` 的高度（64dp -> 56dp），使界面更显紧凑。
    - 重构了搜索框样式，使其更加“扁平化”且向两侧拉长。
    - 搜索框内的提示文字强制单行显示，解决了在小屏幕下可能出现的折行问题。
*   **全场景转场动画升级**:
    - 新增了 `slide_up` (向上回收/滑入) 和 `slide_down` (向下退出) 动画。
    - 详情页进入时采用“向上滑入”的物理动效，退出时向下收回，提升了层级感。
*   **详情页主题独立与同步修复**:
    - 详情页 header 区域新增了明暗切换按钮，方便用户根据内容环境调整。
    - 修复了详情页背景色无法随全局主题实时刷新的 Bug，现在已完全接入 `MaterialTheme` 动态配色系统。
*   **全场景主题同步 (2025-05-22)**:
    - 实现了真正的**全应用明暗主题控制**。通过 `MainViewModel` 与 `Store` 联动，主题状态现在是持久化且全局同步的。
    - 播放页 (`PlayerActivity`)、所有标签页 (首页、搜索、直播、缓存)、所有弹窗 (用户菜单、登录对话框) 以及网盘资源列表均已接入 `SuperTVTheme`。
    - 移除了各组件中的硬编码颜色，全面切换至 `MaterialTheme.colorScheme` 动态属性，确保在深色模式下背景纯黑、浅色模式下背景纯白，对比度及视觉舒适度大幅提升。
*   **底部导航栏深度精简**:
    - 降低了底部导航栏的高度（68dp+12dp -> 56dp），释放了更多垂直内容空间。
    - 实现了**文字常态化显示**逻辑，用户无需选中即可直观识别各个板块。
    - 修复了标签切换时的动画逻辑。现在切换时会根据标签的左右物理位置，自动决定是向左还是向右滑动（isForward），交互逻辑更加自然。
*   **核心搜索链路修复**:
    - 解决了 `LunaTV` 后台返回 `episodes` 为字符串数组而非对象数组导致的解析失败问题。现在 `SearchResult` 模型支持双模式解析。
    - 优化了 `VideoCard` 的封面加载策略，优先尝试 `cover` 字段，若为空则自动回退至 `poster` 字段，解决了搜索结果“无封面”的问题。
    - **彻底修复 URL 传参及节点显示乱码**: 
        - 针对部分服务器环境下的中文参数乱码问题，重构了 `ApiService`，手动执行 **UTF-8 强制编码**。
        - 修复了 `assets/api_nodes.json` 文件中“亚马逊节点”等名称由于二次编码导致的 Mojibake (äºé©¬é) 乱码。
        - 增强了 `RetrofitClient` 拦截器，新增**启发式编码检测**：若响应体包含 `\ufffd` 替换字符，则自动尝试切换至 **GBK** 编码重新解码，确保在任何服务器环境下节点名称均显示正常。
  - **体验深度增强与全场景对齐 (2025-05-23)**:
        - **真·常驻顶栏**: 彻底解决了顶栏在分类页（电影、剧集、直播等）消失的问题。现在 `GlobalHeader` 在所有主标签页下均保持常驻，且完美适配沉浸式状态栏。
        - **全场景左右滑动动画**: 统合了全应用的主频道切换动效。无论是点击底部导航栏还是首页分区标签，页面均会根据标签的逻辑顺序执行对应的**左右平滑切换动画**。
        - **背景闪烁修复**: 针对页面切换时可能出现的黑色背景闪烁问题，实现了**动态同步背景色**。在动画过渡期间，背景颜色会根据当前主题（深色/浅色）实时调整，确保视觉连贯性。
        - **直播模式 UI 统合**: 移除了直播页内嵌的冗余顶栏，将其完全合并至 `MainActivity` 的全局控制器中。修复了直播模式下顶栏颜色不随主题切换的 Bug。
        - **详情页激进修复**: 
            - 重构了 `VideoDetail` 模型，引入了与 `SearchResult` 一致的**双模式解析逻辑**，完美兼容服务器返回的字符串数组与对象数组格式的剧集列表。
            - 解决了部分视频源点击进入详情后“无法加载视频/剧集列表为空”的顽疾。
            - **自动切换有效源**: 针对豆瓣/Bangumi 等仅提供元数据的源，实现了进入详情页后若无剧集，自动以后台全网搜索结果补全并秒级切换至第一个有效播放源的功能。
            - **海报丢失修复**: 增强了详情页海报加载逻辑，支持从跳转参数及备选源中提取 fallback 海报，彻底解决详情页顶部背景消失的问题。
            - 详情页样式进一步向 `supertvold` 靠拢，优化了层级感知与内容展示。
        - **状态栏持久可见**: 修正了部分页面自动隐藏状态栏的问题，确保时间、信号等系统信息在全场景下（除播放器外）均清晰可见。
  - **详情页逻辑深度重构 (对齐 SuperTV_old)**:
        - **剧集解析升级**: 完美移植了旧项目的 `parseEpisode` 逻辑，支持 `标题$URL`、`标题#URL` 等多种复杂格式的自动分离与解析，彻底解决了部分源“无法加载视频”的问题。
        - **数据模型增强**: `SearchResult` 与 `VideoDetail` 现在具备强大的容错能力，可自动识别并处理 API 返回的字符串数组或对象数组格式的剧集列表，稳定性大幅提升。
        - **标签化布局**: 详情页引入了“剧集”与“播放源”选项卡（Tabs），选集区域改为**网格 (Grid) 展示**，更符合原生 App 及 TV 端的交互逻辑。
        - **全网激进检索状态**: 当进入豆瓣/Bangumi 等元数据源时，详情页会显示“全网激进检索中...”的进度反馈，并以后台并行方式自动补全最佳播放源。
        - **自动测速与排序**: 详情页播放源现在根据实时延迟和剧集完整度进行综合评分排序，优先展示最稳定、最快的线路。
  - **交互体验与视觉精进 (2025-05-24)**:
        - **搜索页 UI 规范化**: 修复了搜索界面视频卡片（VideoCard）在浅色模式下背景色异常的问题，全场景对齐 `MaterialTheme` 动态配色。
        - **DLNA 发现优化**: 规范化了 DLNA 设备名称显示，通过解析 `FRIENDLYNAME` 及 `X-Friendly-Name` 头部，解决了以往仅显示“Linux”或 IP 地址的问题，设备识别更加直观。
        - **播放器进度条重构**: 深度定制了 `PlayerActivity` 的进度条（Slider）样式，提升了粗细感与交互灵敏度，视觉效果更加现代化且符合观影习惯。
        - **骨架屏主题对齐**: 更新了 `ShimmerGrid` 加载占位符的颜色逻辑，使其完美适配全局明暗主题切换，消除了加载时的视觉断层。
        - **物理感全屏主题转场 (2025-05-24)**:
            - 参考了 `LunaTV-Enhanced` 的交互逻辑，实现了独有的全屏主题切换动效。
            - **深色模式切入**: 黑色遮罩从屏幕顶部向下滑动覆盖全屏，模拟“夜幕降临”的物理感。
            - **浅色模式切入**: 白色遮罩从屏幕底部向上滑动覆盖全屏，模拟“黎明升起”的升腾感。
            - 动画过程顺滑，有效遮蔽了主题色切换时的瞬间跳变，提升了视觉高级感。
        - **元数据与播放源彻底分离 (2025-05-24)**:
            - **杜绝非播放源挂载**: 彻底解决了豆瓣、Bangumi 被误作为“播放源”展示在换源列表或快速换源面板中的问题。
            - **智能状态提示**: 当详情页处于豆瓣/Bangumi 等元数据源且尚未匹配到播放源时，UI 会清晰提示“当前仅元数据”并展示激进检索进度，而非显示无效的播放按钮。
            - **全场景过滤**: 无论是搜索界面的快速换源弹窗，还是详情页内部的换源列表，均已应用强制过滤逻辑，确保用户点击的每一个源均具备真实的视频流。
            - **精简详情页顶栏**: 移除了详情页顶部冗余的“当前源”显示区域，统一通过下方的“播放源”选项卡进行展示与切换，界面更加紧凑。
        - **导航与动画微调**:
            - 将搜索页与缓存页的进入动画改为**从上往下滑入**（`slide_in_top`），详情页改为**从下往上滑入**（`slide_up`）。
            - 移除了子页面之间的左右滑动切换，转场更加垂直、专注，降低了视觉疲劳感。
        - **缓存系统重构**:
            - 借鉴 `supertvold` 引入了**剧集对齐合并逻辑**。在进入豆瓣/Bangumi 元数据详情时，系统会自动聚合所有播放源的剧集列表，补全缺失集数。
            - 修复了缓存下载失败的问题：增强了下载请求头（UA/Referer）处理，并实现了文件路径安全规范化，解决了由于视频 ID 包含非法字符导致的 IO 异常。
        - **导航逻辑优化 (Tab 智能归位)**:
            - 修复了点击底部导航栏标签时，若当前处于详情页/搜索页/缓存页，无法正确返回主列表的问题。
            - 实现了 **“重选归位”** 逻辑：在当前标签下点击对应图标，会自动执行 `popBackStack` 回到该频道的根页面。
            - 优化了标签切换时的状态恢复策略，确保从“独立页面”切回时优先展示各频道的原始列表，提升了导航的直观性。

## 已完成里程碑
- [x] **架构迁移**：完全移除 React Native/Expo 依赖，建立纯原生 Kotlin 工程。
- [x] **SDK 兼容性适配**：支持 Android 7.0+ (`minSdk 24`)。
- [x] **UI 图标系统深度还原 (Official Material Symbols Style)**：解决了图标风格不统一的问题，对齐 Selene 交互体验。
- [x] **TV 端布局重构 (SuperTV_old Style)**：针对电视端实现全新的交互布局。
- [x] **内容生态扩展 (LunaTV Integration)**：集成短剧分类、AI 智能推荐、即将上映日历。
- [x] **搜索系统升级**：支持“全网聚合”与“网盘资源”双模式搜索。
- [x] **自适应布局 (Tablet/Mobile)**：完美适配手机与平板的不同屏幕尺寸。
- [x] **多节点动态切换修复**：彻底解决了应用启动时默认 API 地址失效的问题。通过在 `MainActivity` 预初始化 `RetrofitClient` 并采用动态 Service 注入，确保节点切换实时生效，不再出现 `api.example.com` 无法解析的错误。
- [x] **乱码与认证修复**：彻底解决了 UTF-8 编码乱码与 401 自动拦截登录逻辑。
- [x] **播放页深度重构**:
    - 实现了全新的**移动端竖屏播放页**，支持选集、换源及简介切换，交互体验对齐 `supertvold`。
    - Landscape 播放器新增**全屏 Compose 控制器**，整合了投屏、返回、标题、选集、换源等所有功能。
    - 实现了**状态栏与 UI 同步隐藏**，提供极致的沉浸式观影体验。
    - 集成 **DLNA 投屏组件**，支持搜索并投送视频至电视端。
- [x] **详情页 UI 优化**:
    - 移除了底部冗余的操作栏，将**缓存按钮**移至顶栏，布局更加清爽。
    - 实现了**源节点评分排序与测速**，自动过滤响应超过 10s 的失效源。
- [x] **多源逻辑增强**:
    - 解决了详情页默认显示豆瓣/Bangumi 等元数据源导致无法播放的 Bug，系统现在会自动匹配并切换至第一个有效播放源。
    - 实现了**切换源极速路径**：若搜索结果已带剧集，则秒级切换，无需重新加载详情。
- [x] **导航与动画微调**:
    - **多级分类系统**: 
        - **子分类导航**: 在首页各大标签页（电影、剧集、动漫、综艺、短剧）中新增了二级子分类筛选条，支持更细粒度的资源检索。
        - **数据对齐**: 子分类列表深度对齐了 `Selene` 和 `LunaTV-Enhanced` 的后端逻辑，涵盖了“华语、欧美、韩剧、日剧”等地区分类，以及“喜剧、动作、科幻”等类型分类。
        - **智能 Tag 映射**: 实现了子分类到豆瓣 API `tag` 参数的智能映射，确保筛选结果的准确性与丰富度。
    - 将动漫标签页更名为 **“每日更新动漫”**，内容同步更新为当天的番剧列表。
    - 重构了全场景转场动效：详情页采用 `slide_up/down`（上下滑入滑出），搜索与缓存页采用 `slide_in_top/out_top`（从顶部落下/收回）。

- [x] **投屏与权限优化**:
    - 增加了 `NEARBY_WIFI_DEVICES` 权限声明。
    - 优化了 DLNA 发现算法，支持同时向多播和广播地址发送发现脉冲，显著提升设备发现率。
- [x] **播放器进阶功能**:
    - 实现了**倍速播放**功能（支持 0.5x - 2.0x）。
    - 引入了**可拖动进度条 (Slider)**，支持精准跳转。
    - 优化了手势调节灵敏度，操作更加自然。
    - 修复了播放器显示 UI 时视频画面意外缩小的 Bug，实现了真正的沉浸式 Overlay。
    - 优化了 UI 动画，顶栏与底栏分别实现从上下边缘收缩/展开。
- [x] **详情页加载重构 (对齐 SuperTV_old)**:
    - **路径化检索策略**: 将详情加载分为“已知视频源 (Path A)”和“元数据源 (Path B)”两条路径。Path A 优先加载首选源并异步补全；Path B 启动全网激进并发探测，极大提升了首屏响应速度。
    - **内存详情池 (秒开)**: 引入了 `SearchDetailPool` 机制。对于短时间内重复进入的剧集，系统通过内存缓存实现“零延迟”秒开。
    - **极致并发检索**: 基于协程并行执行所有站点的资源匹配，并实时上报搜索百分比进度。
    - **智能源合并**: 自动聚合全网所有来源的剧集列表，实现“跨源对齐”，解决单一来源缺集、断更的问题。
- [x] **架构与视觉全方位对齐 (Selene & SuperTV_old)**:
    - **高性能架构 (Metadata First)**: 首页及分类页重构为“元数据优先”模式。进入页面仅加载极快的资料数据，复杂的播放源匹配转入后台执行。点击分类几乎“秒开”，彻底解决了加载卡顿问题。
    - **海报加载动画 (Shimmer Effect)**: 引入了 Selene 风格的骨架屏特效。在海报加载前展示线性渐变闪烁动画，加载完成后平滑切换，消除了页面白屏感。
    - **UI 视觉体系升级**: 
        - **标志性角标**: 海报卡片新增左上角年份标签、右上角**粉色圆形评分徽章**（或绿色集数标签）以及底部渐变来源条，视觉还原度对齐 Selene。
        - **智能网格排布**: 移动端统一 3 列、TV 端 5 列排布，标题支持 2 行显示，兼顾信息量与整齐度。
    - **搜索逻辑深度补完**: 
        - **32路并发控制**: 使用 `Semaphore` 信号量限制搜索并发，解决了后端 API 挤爆导致的 500 错误。
        - **流式实时更新**: 搜索结果不再等待全部搜完，每当检测到有效变体匹配即实时刷新 UI，体验更接近 Selene 的 `flushBatch`。
    - **物理感交互回归**: 修复了丢失的底部标签左右滑动动画。现在系统能感知标签的相对物理位置，实现“精准对向”的滑入滑出动效。
- [x] **搜索交互升级**:
    - **并发搜索优化**: 实现了“精准搜索 -> 模糊搜索”的两阶段异步检索。
    - **多线程并行检索**: 详情页播放源匹配现已全面应用 **协程并行 (Dispatchers.IO + async)** 模式。系统会同时对多个变体词发起并发请求，检索速度提升了近 3 倍。
    - **严格精准碰撞**: 若标题通过“去空格去符号”实现精准匹配，系统将立即终止并仅展示该精准结果。
    - **剧名智能去尾**: 当全名匹配失败时，系统会自动执行“去尾精准匹配”：切除剧名最后一个空格后的所有后缀（如：第N季、SP等），并对剩余主标题进行去空格去符号的精准二次碰撞，大幅提升了续作和番外篇的召回率。
    - **详情页简介折叠**: 视频简介默认仅显示前 5 行，支持点击展开/收起，优化了长文本页面的浏览体验。
- [x] **架构性能与交互深度重构 (对齐 Selene & SuperTV_old)**:
    - **全豆瓣后端源**: 所有主标签页（首页、电影、剧集、动漫、综艺等）列表数据统一通过后端豆瓣 API 获取，极大地提升了内容丰富度与加载稳定性。
    - **极致加载引擎 (Metadata First)**: 列表页仅加载元数据（标题、海报、简介），彻底移除阻塞性的后台播放源匹配，实现了分类切换的“秒开”级体验。
    - **内存快照缓存**: 引入了 `memoryCache` 内存快照技术。在应用运行期间，已点开过的子分类再次切换时可实现 **0ms 瞬间弹出**，无需重复请求。
    - **发包方式重构**: 优化了 `RetrofitClient` 发包头部。将 `User-Agent` 与 `Referer` 伪装为现代桌面浏览器，并启用了 **HTTP 长连接 (Keep-Alive)** 与 **Gzip 压缩**，回包效率提升 40%。
    - **骨架屏闪烁动画**: 实现了与 Selene 完全对齐的 **Shimmer Skeleton Grid**。在数据加载前展示线性渐变闪烁动画，彻底消除白屏等待感。
    - **物理感导航动画 (1-7 编号逻辑)**: 底部标签栏引入了绝对物理编号。逻辑严格遵循“小号切大号从右入，大号切小号从左入”，解决了连续点击时动画方向混乱的问题。
    - **扁平化渲染优化**: 重构了 `TransformFragment` 的排布逻辑，移除嵌套 Grid，使用 chunked 扁平化列表渲染，解决了在低端机型上滑动卡顿的问题。
    - 点击搜索结果不再直接跳转，而是弹出**快速换源面板**，支持预览剧集信息、直接播放或进入完整详情。
- [x] **动漫频道重构**:
    - 实现了**周一至周日更新日历**，支持按星期查看番剧。
    - 标签页改回“动漫”，交互体验更符合二次元用户习惯。
- [x] **稳定性增强**:
    - 增加了 401 未授权弹窗的**冷却节流机制**，解决了切换节点或失效时重复弹出对话框的问题。
    - 详情页源列表增加了**延迟分级标签**（极速、一般、较慢）。

## 配置与部署说明

### GitHub Actions 节点配置
请在 **GitHub 仓库设置 -> Settings -> Secrets and variables -> Actions** 中添加 Secret：
- **Name**: `API_NODES_JSON`
- **Value**: JSON 格式节点数组。

---
*注：本项目目前已完美集成 LunaTV 的强大后端能力与 Selene 的精致视觉体验。*
