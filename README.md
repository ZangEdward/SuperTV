# SuperTV 原生安卓重构项目 (LunaTV & Selene 增强版)

本项目致力于将基于 Expo/React Native 的 `supertv` 项目重构为**现代 Android 原生应用 (Kotlin + Jetpack Compose + Material3)**。

## 目录结构
```text
app/src/main/
├── java/com/supertv/app/
│   ├── ui/                 # UI 层 (Compose)
│   │   ├── components/     # 共享 UI 组件 (已实现 Paging 3, UserMenu, AI对话框)
│   │   ├── transform/      # 首页分区布局 (带 Lucide 分类导航)
│   │   ├── search/         # 搜索界面 (双模式：聚合 & 网盘)
│   │   ├── detail/         # 详情界面
│   │   ├── player/         # 播放器界面 (手势控制与选集弹窗)
│   │   └── slideshow/      # 缓存管理界面 (原生任务列表)
│   ├── data/               # 数据层 (Retrofit, Store, ApiNodeService)
│   ├── services/           # 原生业务逻辑服务 (Search/M3U/AdFilter/Dlna/AI/CrashHandler)
│   └── model/              # 数据模型定义 (集成 LunaTV 增强模型)
├── res/                    # 资源文件 (基于 Lucide 风格重制的图标系统)
└── assets/                 # 动态注入配置 (api_nodes.json)
```

## 最新更新与修复
- [x] **搜索并发优化**: 重构了 `SearchEngine`，采用 Kotlin 协程 `async` 并发请求所有视频源，极大地提升了搜索速度，解决了获取信息慢的问题。
- [x] **网盘搜索修复**: 更新了 `NetDiskResponse` 模型以对齐 LunaTV 接口，现在“盘搜”功能可以正常解析并展示搜索结果。
- [x] **乱码彻底修复**: 在 `RetrofitClient` 中引入了强制 UTF-8 解码的拦截器，解决了服务器节点名称在 Android 原生端显示乱码的问题。
- [x] **导航与详情页修复**: 
    - 实现了点击影视海报进入详情页的完整导航逻辑。
    - 详情页播放源采用**竖向滑动排布**，优化了在大屏幕和列表下的选择体验。
- [x] **全局 Header 深度重构**: 
    - 移除了顶部的 APP 名称，改为居中式的**现代化搜索框**。
    - 左侧集成**用户头像/菜单**，点击可进入功能丰富的用户中心。
    - 右侧集成**下载管理按钮**，快速查看离线任务。
    - 新增**明暗主题切换**功能，支持深色与浅色模式的一键无缝转换。
- [x] **全场景转场动画**: 修复了页面切换动画缺失的问题。现在所有页面跳转（包括底部标签切换、详情页进入、搜索打开等）均支持平滑的**左右滑动效果**（首页切换除外，保持稳定感）。
- [x] **直播页修正**: 将原本显示错误的直播页重构为**电视直播 (TV Live)**，整合了央视与主流卫视频道列表。
- [x] **用户菜单功能完备**: 仿照 Selene 完善了用户中心，支持**观看历史**、**我的收藏**、**节点测速切换**、**AI 智能推荐**以及**上映日历**等核心功能。
- [x] **高性能缓存 (秒开)**: 引入了基于 `SharedPreferences` 的数据持久化层，首页及分类频道实现**秒级启动加载**，并在后台静默刷新数据。
- [x] **登录逻辑收紧**：移除了本地演示模式（跳过按钮），强制用户登录以访问受限资源。
- [x] **用户信息显示**：修复了点击头像后用户菜单中用户名显示不正确的问题，现在能正确显示当前登录用户。
- [x] **搜索稳定性提升**：
    - 修复了无网络环境下点击搜索按钮导致的闪退（Crash）。
    - 修复了搜索结果无法正常显示的问题，提升了搜索的响应率。
- [x] **API 链路优化**：解决了底部标签数据加载 404 的问题，统合了 API 路由规则。
- [x] **UI 极致精简**：移除了首页悬浮的信封按钮，界面更加清爽。
- [x] **导航逻辑修复**：全局适配物理返回键/手势返回，确保所有界面按返回键均能正确回退至上一级页面。
- [x] **服务器测速还原**：同步 `supertvold` 的测速逻辑，使用客户端原生 `HEAD`探测，解决了节点提示“不可达”的问题，提供实时的延迟反馈。
- [x] **核心搜索逻辑对齐**: 
    - 引入了 `SearchUtils`，完美复刻 `supertvold` 的去空格、去符号及结果合并逻辑。
    - 实现了“精准匹配 -> 去尾搜索”的两阶段激进检索策略，显著提升了资源召回率。
- [x] **元数据优先聚合**: 
    - 首页及分类页优先加载豆瓣/Bangumi 的精美元数据（海报、评分）。
    - 自动触发后台异步匹配，将元数据项目与自有 API 播放源关联，解决了纯元数据源无法播放的问题。
- [x] **全局主题与 UI 优化**:
    - **真全局深色模式**: 重构了 `MainActivity` 布局，所有顶栏、底栏及容器均完美适配明暗切换，解决了搜索栏背景色不跟随的问题。
    - **底栏无缝衔接**: 修复了底部导航栏容器在部分机型上出现的漏角/边缘偏移问题，实现了全屏铺满效果。
    - **自适应栅格布局**: 分类页网格列数现在根据屏幕方向（横屏/竖屏）自动调整（3列或5列），提供更佳的视觉体验。
- [x] **数据统计中心**: 新增“数据统计”功能，可实时查看收藏数、历史记录数、搜索次数及缓存集数。
- [x] **盘搜功能大升级**: 
    - 仿照 `supertvold` 实现了网盘资源分类，支持夸克、百度、阿里、磁力等多种资源的一键切换。
    - 新增“复制链接”与“直接打开”功能，完美对接移动端浏览器与下载器。
- [x] **移动端视觉统一**: 
    - 重构了移动端搜索卡片，将原本拥挤的 3 列布局优化为 2 列大图模式。
    - 统一了海报角标样式（评分、来源、集数），视觉观感对齐 Selene 风格。
- [x] **全网激进检索 (详情页增强)**: 
    - 解决了豆瓣/Bangumi 等纯数据源无法播放的问题。进入此类详情页时，App 会自动以后台方式触发全网同名资源搜索。
    - 支持全网源自动合并与测速，智能提示“全网检索中...”，并自动加载第一个有效播放源。
- [x] **智能测速选集**: 详情页新增播放源实时测速功能，直接在源列表显示延迟（ms），并根据快慢进行颜色分级，确保极速观影。
- [x] **缓存持久化优化**: 将离线下载目录从系统临时目录迁移至应用专用外部存储空间，解决了应用关闭或系统清理时缓存丢失的问题。
- [x] **全方位稳定性保障**:
    - **导航容错**: 优化了 Navigation Action 的分发逻辑，彻底修复在首页分类频道点击剧集卡片导致的闪退问题。
    - **乱码深度兼容**: `ApiNodeService` 新增启发式编码检测，支持 UTF-8 与 GBK (ANSI) 自动切换。
    - **开发者知识库 (上下文记忆)**: 记录了核心逻辑架构及避坑指南。
- [x] **UI 布局深度优化**:
    - **彻底解决双顶栏**: 移除 Fragment 内部渲染的冗余 Header，统一由 `MainActivity` 控制全局固定顶栏。
    - **首页沉浸式修复**: 修正了首页分类内容被顶栏遮挡或出现重复偏移的问题。
    - **分类页视觉升级**: 除了“热门”外，电影、剧集、动漫、综艺及短剧频道均改为 **3 列网格 (Grid)** 竖向展示，浏览效率大幅提升。
    - **动画物理感增强**: 修复并增强了底部导航栏与首页分类标签的左右滑动动画，方向完全匹配标签顺序。
- [x] **播放源逻辑补全**: 进一步对齐 `supertvold`，确保豆瓣/Bangumi 等元数据源在进入详情页时能正确通过全网搜索补全播放源。

## 开发者知识库 (上下文记忆)

为了方便后续维护和上下文清除后的快速恢复，以下是本项目核心逻辑的“阅读经验”：

### 1. 架构关联
*   **后端对齐**: 本项目逻辑深度参考了 `.gradle/SuperTV_old-master`。`SearchViewModel` 对应原项目的 `stores/netdiskStore` 和 `stores/searchStore`。
*   **数据流**: 搜索采用 **Paging 3** 分页。网盘搜索结果是 `Map<String, List<NetDiskItem>>`，需要按 Key 分类展示。
*   **双模式详情**: 豆瓣/Bangumi 源仅提供元数据。进入详情页时，必须调用 `SearchViewModel.loadAllSources()` 触发全网激进检索，以补全播放链接。

### 2. 核心避坑指南
*   **编码问题**: `api_nodes.json` 经常在 Windows 下被保存为 GBK。`ApiNodeService` 必须包含 `\ufffd` 启发式检测或多重编码尝试读取。
*   **图片防盗链**: 豆瓣图片必须带 `Referer: https://movie.douban.com/` 和特定 UA，由 `ImageUrlHelper` 统一处理。
*   **导航陷阱**: 本项目多个标签页共享 `TransformFragment`。在 XML 中定义的 Action（如 `action_nav_tv_to_detail`）必须与当前所在的 `id` 匹配，否则会抛出 `IllegalArgumentException`。在 `onItemClick` 中应动态判断 `findNavController().currentDestination?.id`。
*   **沉浸式适配**: `GlobalHeader` 在搜索和详情页应隐藏。底部 `ComposeBottomNavBar` 需要手动处理 `windowInsetsPadding(WindowInsets.navigationBars)` 且背景 `Surface` 需额外高度以防系统小白条处漏色。

### 3. 动画逻辑
*   **方向感交互**: 页面切换动画（`slide_in/out`）并非固定。在 `MainActivity` 和 `TransformFragment` 中，通过对比 `targetIndex` 与 `currentIndex` 来决定是从右往左还是从左往右滑。

### 4. 数据源与播放源逻辑 (2025-05-22 更新)
*   **数据源分类**: 豆瓣、Bangumi 仅作为**元数据源**（提供海报、评分、简介），视频 CMS 站点作为**播放源**。
*   **详情页补全策略**: 进入详情页时，若 `source` 为 `douban` 或 `bangumi`，客户端改用专用接口（`/api/douban/details` 或 `api/proxy/bangumi`）获取元数据，并同步触发全网搜索。
*   **自动合并**: 全网搜索结果会自动与当前元数据详情合并。若当前详情无剧集（数据源），将自动取第一个搜索结果的剧集列表进行填充，实现了“点开豆瓣海报即刻搜源播放”的无缝体验。
*   **激进搜索算法增强**: 改进了 `SearchUtils.generateSearchVariants`。支持“空格消除”、“中文数字归一化（如：第四季 -> 第4季）”、“渐进式词组截断”等多种变体并行搜索，大幅提升了资源命中率，解决了“搜索搜不到”的问题。
*   **网盘搜索修复**: 完全兼容 `LunaTV-Enhanced` 后台的 `merged_by_type` 数据结构。修复了 `NetDiskResponse` 解析失败导致的结果为空问题，现在支持按云盘类型（阿里、夸克、百度等）分类展示结果。
*   **TV 端搜索对齐**: 解决了 TV 端点击海报无法进入详情或进入后无剧集的问题，逻辑现在与移动端保持一致。

## 已完成里程碑
- [x] **架构迁移**：完全移除 React Native/Expo 依赖，建立纯原生 Kotlin 工程。
- [x] **SDK 兼容性适配**：支持 Android 7.0+ (`minSdk 24`)。
- [x] **UI 图标系统深度还原 (Official Material Symbols Style)**：解决了图标风格不统一的问题，对齐 Selene 交互体验。
- [x] **TV 端布局重构 (SuperTV_old Style)**：针对电视端实现全新的交互布局。
- [x] **内容生态扩展 (LunaTV Integration)**：集成短剧分类、AI 智能推荐、即将上映日历。
- [x] **搜索系统升级**：支持“全网聚合”与“网盘资源”双模式搜索。
- [x] **自适应布局 (Tablet/Mobile)**：完美适配手机与平板的不同屏幕尺寸。
- [x] **乱码与认证修复**：彻底解决了 UTF-8 编码乱码与 401 自动拦截登录逻辑。

## 待办事项
1. [ ] **播放器控制进一步优化**：针对绿色主题进行微调。
2. [ ] **多源负载均衡**：对接更多的备用 API 自动切换逻辑。

## 配置与部署说明

### GitHub Actions 节点配置
请在 **GitHub 仓库设置 -> Settings -> Secrets and variables -> Actions** 中添加 Secret：
- **Name**: `API_NODES_JSON`
- **Value**: JSON 格式节点数组。

---
*注：本项目目前已完美集成 LunaTV 的强大后端能力与 Selene 的精致视觉体验。*
